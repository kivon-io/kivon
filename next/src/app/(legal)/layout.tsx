import LegalLayout from "@/components/dynamic-zone/legal/legal-layout"

const Layout = ({ children }: { children: React.ReactNode }) => {
  return <LegalLayout>{children}</LegalLayout>
}

export default Layout
